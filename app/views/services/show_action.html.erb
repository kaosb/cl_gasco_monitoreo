<div class="box">
	<div class="box-header with-border">
		<h3 class="box-title"><%=@action.name%></h3>
	</div>
	<div class="box-body">
		<%= line_chart @log.group_by_hour(:created_at).group(:response_time).count, {library: {hAxis: {title: "Hora"}, vAxis: {title: "Tiempo respuesta"}}} %>
	</div>
	<div class="box-footer">
	</div>
</div>
